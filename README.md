# Task Assigner

Simple demo webapp for supervisors to assign tasks to employees across four timeslots.

Run:

```
npm install
npm start
```

Open:
- Admin: http://localhost:3000/admin (login password: `admin`)
- Dashboard: http://localhost:3000/

This app stores data in memory for demo purposes. For production, add a persistent database and proper auth.
